<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Keep the screen awake during a set (parity with the PWA's Wake Lock). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Haptics for work/rest/safety cues. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- OPTIONAL heart rate. Declared, but only REQUESTED at runtime when the
         user turns on the informational HR setting. Denying it must not block
         the workout (see WatchSettings / HeartRateController). -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Standalone: the watch app works even without the phone app installed. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receives session / block / swing sync + control messages from the phone. -->
        <service
            android:name=".data.Kb30WearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/kb30" />
            </intent-filter>
        </service>
    </application>
</manifest>
